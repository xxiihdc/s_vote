# Todo List cho feature: 004-anonymous-voting

- [x] Bước 1: Tạo bản đặc tả (Specify) → dùng /speckit.specify
- [x] Bước 2: Tinh chỉnh bản đặc tả (Clarify) → dùng /speckit.clarify
- [x] Bước 3: Lập kế hoạch triển khai kỹ thuật (Plan) → dùng /speckit.plan
- [ ] (Tùy chọn) Xác thực kế hoạch → dùng /speckit.analyze
- [x] Bước 4: Chia nhỏ công việc (Tasks) → dùng /speckit.tasks
- [x] Bước 5: Triển khai (Implement) → dùng /speckit.implement
